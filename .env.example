DATABASE_URL="postgresql://postgres:postgres@localhost:5432/notebookllm?schema=public"
OPENAI_API_KEY=""
QDRANT_URL="http://localhost:6333"
QDRANT_API_KEY=""
# Required for sign-in and for /api/chat's auth() check (see src/app/layout.tsx,
# src/middleware.ts). Get these from a Clerk application's API Keys page.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=""
CLERK_SECRET_KEY=""
# Set to "true" once `npm run setup:qdrant` has confirmed the collection and its
# payload indexes exist, so serverless cold starts skip re-checking Qdrant.
QDRANT_COLLECTION_READY=""
